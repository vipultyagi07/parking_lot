server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parking_lot
    username: root
    password: vipul1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vipultyagicoc@gmail.com
    password: stpc donm gdiu dydq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

parkinglot:
  email:
    senderName: "Parking Lot"

otp:
  encryption:
    secretKey: AVdvd@1999#d
